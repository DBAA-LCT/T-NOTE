# T-Note

<div align="center">

![T-Note Logo](build/icon-256x256.png)

一款功能强大的本地富文本笔记编辑器，支持多页面管理、云同步、标签系统、分屏编辑等功能

[![Version](https://img.shields.io/badge/version-2.1.5-blue.svg)](https://github.com/DBAA-LCT/T-NOTE)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Electron](https://img.shields.io/badge/Electron-28-47848F.svg)](https://www.electronjs.org/)
[![React](https://img.shields.io/badge/React-18-61DAFB.svg)](https://reactjs.org/)

[功能特性](#功能特性) • [快速开始](#快速开始) • [使用指南](#使用指南) • [完整手册](docs/USER_GUIDE.md) • [技术栈](#技术栈) • [开发](#开发)

</div>

---

## 功能特性

### 📝 富文本编辑
基于 Quill 2.0 编辑器，提供强大的富文本编辑能力：
- 多级标题（H1/H2/H3）
- 文本样式：粗体、斜体、下划线、删除线
- 列表：有序列表、无序列表
- 颜色：文字颜色、背景色
- 对齐方式、链接、图片、代码块、引用
- 长代码块独立存储，优化性能

### 📄 多页面管理
- 在单个笔记中创建多个独立页面
- 每个页面拥有独立的标题、内容和标签
- 页面列表显示标题、标签和更新时间
- 支持页面删除和回收站恢复

### 🏷️ 智能标签系统
- 为每个页面添加多个彩色标签
- 按标签快速搜索和过滤页面
- 标签可视化显示，易于识别和管理

### 📑 Tab 栏和分屏编辑
- **双 Tab 栏设计**：左右两侧各有独立的 Tab 栏，最多显示 5 个 Tab
- **灵活的分屏操作**：
  - 拖拽 Tab 到分屏区域
  - 右键菜单快速分屏
  - 向右拖动 Tab 超过 100px 自动分屏
- **焦点切换机制**：同一时间只有一个编辑器可编辑
- **独立编辑**：左右分屏独立工作，修改实时保存

### 🔖 书签功能
- 在编辑器中任意位置添加书签标记
- 书签面板显示所有书签及其备注
- 点击书签快速定位到对应位置
- 支持书签编辑和删除

### 🔍 全文搜索
- 按标签搜索页面
- 按内容搜索页面
- 实时过滤显示结果
- 快速定位到搜索结果位置

### ✅ TODO 管理
- 添加待办事项（标题、描述、优先级、分类、截止日期）
- 完成/取消完成、编辑、删除
- 按状态和优先级过滤
- 优先级标识（高/中/低）
- 截止日期提醒
- 自动排序和分类

### ☁️ 云同步功能
- **多账号支持**：同时管理多个云盘账号
- **支持的云盘**：
  - OneDrive
  - 百度网盘
- **同步功能**：
  - 自动同步和手动同步
  - 冲突检测和解决
  - 离线模式支持
  - WiFi 限制选项
- **云端笔记管理**：
  - 浏览云端笔记列表
  - 预览模式（无需下载到本地）
  - 快速上传和下载
  - 云端删除和管理

### 🗑️ 回收站
- 删除的页面、书签、TODO 自动进入回收站
- 支持一键恢复
- 支持永久删除
- 支持清空回收站

### 💾 文件操作
- 打开已保存的笔记文件（.note 格式）
- 保存笔记（Ctrl+S 快捷键）
- 另存为新文件
- 本地文件存储，数据安全
- 支持双击 .note 文件直接打开
- 最近打开的笔记列表

### 🔄 自动更新
- 自动检测新版本
- 支持多个更新源（GitHub、Gitee、Cloudflare）
- 后台下载更新
- 一键安装更新

---

## 快速开始

### 安装

从 [Releases](https://github.com/DBAA-LCT/T-NOTE/releases) 页面下载最新版本的安装包：

- Windows: `T-Note-Setup-{version}.exe`

双击安装包，按照提示完成安装。

### 首次使用

1. 启动 T-Note 应用
2. 点击"新建笔记"创建第一个笔记
3. 点击"新建页面"创建第一个页面
4. 输入标题和内容
5. 使用 Ctrl+S 或点击"保存"按钮保存

---

## 使用指南

📖 **完整使用手册**：[编辑器使用说明文档](docs/USER_GUIDE.md)

本手册包含详细的功能介绍、操作步骤、使用技巧和常见问题解答。

### 基本操作

#### 创建和管理笔记
- **新建笔记**：点击顶部菜单"文件" → "新建笔记"
- **打开笔记**：点击"文件" → "打开笔记"，或直接双击 .note 文件
- **保存笔记**：Ctrl+S 或点击"文件" → "保存"
- **另存为**：点击"文件" → "另存为"

#### 页面管理
- **新建页面**：点击左侧"页面"面板的"新建页面"按钮
- **切换页面**：点击页面列表中的页面，或点击 Tab 栏中的 Tab
- **删除页面**：点击页面右侧的删除按钮（会进入回收站）

#### Tab 栏操作
- **打开 Tab**：从左侧页面列表点击页面
- **切换页面**：点击 Tab 栏中的任意 Tab
- **关闭 Tab**：点击 Tab 右侧的 × 按钮
- **分屏显示**：
  - 右键点击 Tab，选择"分屏显示"
  - 拖动 Tab 到右侧的"拖到这里分屏"区域
  - 将 Tab 向右拖动超过 100px 后松开
- **关闭分屏**：点击右上角的"关闭分屏"按钮

#### 云同步设置
1. 点击左侧"设置"图标
2. 选择"云存储账号"
3. 点击"添加账号"，选择云盘类型
4. 按照提示完成授权
5. 设置同步文件夹
6. 开始同步

### 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+S | 保存笔记 |
| Ctrl+N | 新建笔记 |
| Ctrl+O | 打开笔记 |

---

## 技术栈

### 前端
- **框架**：React 18
- **UI 组件库**：Ant Design 6.0
- **图标库**：@ant-design/icons
- **富文本编辑器**：React Quill 2.0 (基于 Quill 2.0)
- **日期处理**：dayjs

### 桌面端
- **框架**：Electron 28
- **日志**：electron-log
- **自动更新**：electron-updater

### 构建工具
- **构建工具**：Vite 5
- **语言**：TypeScript 5
- **打包工具**：electron-builder

---

## 开发

### 环境要求

- Node.js >= 16
- npm >= 8

### 安装依赖

```bash
npm install
```

### 开发运行

```bash
npm run dev
```

这将同时启动：
- Vite 开发服务器（渲染进程）
- Electron 主进程

### 构建

```bash
npm run build
```

### 打包

```bash
npm run package
```

打包后的文件将输出到 `release` 目录。

### 项目结构

```
T-Note/
├── electron/              # Electron 主进程代码
│   ├── main.ts           # 主进程入口
│   ├── preload.ts        # 预加载脚本
│   ├── ipc/              # IPC 通信处理
│   ├── services/         # 服务层（云同步、文件管理等）
│   └── utils/            # 工具函数
├── src/                  # React 渲染进程代码
│   ├── components/       # React 组件
│   ├── hooks/            # 自定义 Hooks
│   ├── types/            # TypeScript 类型定义
│   ├── utils/            # 工具函数
│   ├── App.tsx           # 主应用组件
│   └── main.tsx          # 渲染进程入口
├── build/                # 构建资源（图标等）
├── dist/                 # 构建输出目录
├── release/              # 打包输出目录
└── package.json          # 项目配置
```

---

## 数据结构

### 笔记文件（.note）

```typescript
interface Note {
  id: string;
  name: string;
  pages: Page[];
  todos?: TodoItem[];
  trash?: DeletedItem[];
  createdAt: number;
  updatedAt: number;
  syncConfig?: {
    enabled: boolean;
    autoCommit: boolean;
    oneDrivePath: string;
    lastSyncAt: number;
  };
  cloudSource?: CloudSource;
}
```

### 页面

```typescript
interface Page {
  id: string;
  title: string;
  content: string;
  tags: string[];
  bookmarks?: Bookmark[];
  longCodeBlocks?: Record<string, { content: string; language: string; title: string }>;
  createdAt: number;
  updatedAt: number;
  syncStatus?: {
    status: 'not_synced' | 'synced' | 'pending' | 'syncing' | 'error';
    lastSyncAt?: number;
    cloudUpdatedAt?: number;
    contentHash?: string;
  };
}
```

### TODO 项

```typescript
interface TodoItem {
  id: string;
  title: string;
  description?: string;
  completed: boolean;
  priority: 'low' | 'medium' | 'high';
  category?: string;
  dueDate?: number;
  linkedPageId?: string;
  linkedPosition?: number;
  createdAt: number;
  updatedAt: number;
}
```

---

## 注意事项

1. **Tab 栏限制**：Tab 栏最多显示 5 个，建议及时关闭不需要的 Tab
2. **分屏编辑**：分屏时两个编辑器独立工作，修改会实时保存
3. **页面删除**：删除页面会自动关闭对应的 Tab 和分屏
4. **数据存储**：TODO 和书签数据存储在笔记文件中
5. **云同步**：首次同步建议在 WiFi 环境下进行
6. **备份建议**：建议定期备份重要笔记

---

## 故障排除

### 分屏白屏
- 检查控制台错误
- 清除缓存重新构建

### 样式显示不正常
- 清除缓存后重新运行 `npm run dev`

### 保存失败
- 检查文件路径是否有写入权限
- 检查磁盘空间是否充足

### 云同步失败
- 检查网络连接
- 重新授权云盘账号
- 查看错误日志

---

## 贡献

欢迎提交 Issue 和 Pull Request！

---

## 许可证

[MIT License](LICENSE)

---

## 联系方式

- GitHub: [DBAA-LCT/T-NOTE](https://github.com/DBAA-LCT/T-NOTE)
- Issues: [提交问题](https://github.com/DBAA-LCT/T-NOTE/issues)

---

<div align="center">

Made with ❤️ by DBAA-LCT

</div>
